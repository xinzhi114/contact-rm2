import { IFirebaseConfig } from '../constants/firebase'

export const PRODUCTION_ONLY = false

// For Firebase JS SDK v7.20.0 and later, measurementId is optional
export const firebaseConfig: IFirebaseConfig | null = null
